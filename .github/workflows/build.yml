name: Build and Test (with PIT guard)

on:
  push:
  pull_request:

jobs:
  build-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< Updated upstream
=======
      - uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java-version }}
          distribution: temurin
      - name: Cache Maven artifacts
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - name: Cache node
        uses: actions/cache@v4
        with:
          path: web-bundle/node
          key: ${{ runner.os }}-node-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os}}-node-
      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: web-bundle/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/pom.xml', '**/package.json') }}
          restore-keys: |
            ${{ runner.os}}-node_modules-
      - name: Build ${{ matrix.java-version }}
        run: mvn -B clean test
      - name: Run mutation tests
        run: mvn org.pitest:pitest-maven:mutationCoverage

      - name: Extract mutation score
        run: |
          SCORE=$(grep -oP 'Mutation score.*?\K[0-9]+' target/pit-reports/*/index.html | head -n 1)
          echo "Current mutation score: $SCORE"
          echo "$SCORE" > current-mutation-score.txt

      - name: Download previous mutation score artifact
        uses: actions/download-artifact@v3
        with:
          name: pit-score
          path: previous-pit || echo "No previous artifact"

      - name: Compare with previous score
        run: |
          PREVIOUS_FILE=$(ls previous-pit/*.txt 2>/dev/null || echo "")
          if [ -z "$PREVIOUS_FILE" ]; then
            echo "No previous score found, skipping comparison."
          else
            PREVIOUS=$(cat "$PREVIOUS_FILE")
            CURRENT=$(cat current-mutation-score.txt)
            echo "Previous score: $PREVIOUS"
            echo "Current score: $CURRENT"
            if [ "$CURRENT" -lt "$PREVIOUS" ]; then
              echo "Mutation score decreased! Failing build."
              exit 1
            fi
          fi

      - name: Upload current mutation score as artifact
        uses: actions/upload-artifact@v3
        with:
          name: pit-score
          path: current-mutation-score.txt
>>>>>>> Stashed changes

      - name: Setup JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      # Build uniquement 'core' (on saute web-bundle et autres)
      - name: Build core (skip tests)
        run: |
          mvn -B -q -pl core -am \
            -DskipTests -DskipITs \
            -Dskip.npm=true -Dskip.web=true -DskipNative=true \
            clean install

  pit-verify:
    needs: build-core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 21 (Temurin)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      # Variante A : Run PIT sur le module 'core' uniquement
      - name: Run PIT on core
        run: |
          mvn -B -pl core -am -DskipITs -Dskip.npm=true -Dskip.web=true -DskipNative=true test
          mvn -B -pl core -am \
            -DwithHistory \
            -DreportsDirectory=core/target/pit-reports \
            org.pitest:pitest-maven:mutationCoverage

      - name: Compute current PIT score
        run: |
          set -e
          mkdir -p pit-current
          REPORT_DIR=$(ls -d core/target/pit-reports/* | head -n1)
          cp "$REPORT_DIR/mutations.csv" pit-current/mutations.csv

          TOTAL=$(tail -n +2 pit-current/mutations.csv | wc -l | tr -d ' ')
          KILLED=$(grep -c ',KILLED,' pit-current/mutations.csv || true)

          echo "TOTAL=$TOTAL"    > pit-current/pit-score.txt
          echo "KILLED=$KILLED" >> pit-current/pit-score.txt
          if [ "$TOTAL" -gt 0 ]; then
            awk -v k="$KILLED" -v t="$TOTAL" 'BEGIN{printf("LINE_COVERAGE=%.2f\n",(k/t)*100)}' >> pit-current/pit-score.txt
          else
            echo "LINE_COVERAGE=0.00" >> pit-current/pit-score.txt
          fi
          cat pit-current/pit-score.txt

      - name: Download previous PIT artifact
        id: dl
        uses: dawidd6/action-download-artifact@v3
        with:
          workflow: build.yml
          name: pit-latest
          path: pit-previous
          branch: ${{ github.ref_name }}
          workflow_conclusion: success
          if_no_artifact_found: ignore

      - name: Compare current vs previous (fail on regression)
        run: |
          set -e
          if [ ! -f pit-previous/pit-score.txt ]; then
            echo "::notice::No previous PIT baseline found (first run) -> skipping regression check."
            exit 0
          fi

          CUR=$(grep LINE_COVERAGE pit-current/pit-score.txt  | cut -d= -f2)
          PREV=$(grep LINE_COVERAGE pit-previous/pit-score.txt | cut -d= -f2)

          echo "Previous: ${PREV}%"
          echo "Current : ${CUR}%"

          awk -v cur="$CUR" -v prev="$PREV" 'BEGIN{
            if (cur+0.0001 < prev) {
              printf("Mutation score regressed: current %.2f < previous %.2f\n", cur, prev);
              exit 1
            } else {
              printf(" OK: current %.2f >= previous %.2f\n", cur, prev);
              exit 0
            }
          }'

      - name: Upload current PIT artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pit-latest
          path: pit-current/*
